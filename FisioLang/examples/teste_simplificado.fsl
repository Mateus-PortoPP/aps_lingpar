sessao {
  /* Este é um comentário que será ignorado pelo compilador */
  x = 10;
  y = 20;
  resultado(x + y);
  
  se melhorou(x > 5) {
    resultado(100);
  } caso contrario {
    resultado(200);
  }
  importar_dados("../examples/physical_therapy_exercises.csv");
  valor = ler_sensor("amplitude");
  resultado(valor);
  m = melhora();
  resultado(m);
  
  total = calcular_consultas(x, y, 5);
  resultado(total);
  
  registrar(m, valor);
  
  pausa(2);
}
fim_sessao


